export type DietSelectOption = {
  id: number;
  label: string;
  isInDiet: boolean;
  color: string;
};

export type DateTimePickerOption = {
  mode: "date" | "time";
  label: string;
  title: string;
};
