export type DietSelectOption = {
  id: number;
  label: string;
  isInDiet: boolean;
  bordercolor: string;
  bgColor: string;
};

export type DateTimePickerOption = {
  mode: "date" | "time";
  label: string;
  title: string;
};
